defaultEntrypoints = ["http"]

logLevel = "DEBUG"

[entrypoints]
  [entrypoints.http]
  address = ":8000"

[api]

[docker]

# It's dynamagic !
endpoint = "{{.DockerHost}}"

domain = "docker.localhost"
exposedbydefault = true
